using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pickup_Object : MonoBehaviour
{
    public float Object;
    public float Player;

    // Update is called once per frame
    void Update()
    {
        if(Player = Object)
        {
            Object.transform.postion;
        }
    }
}
